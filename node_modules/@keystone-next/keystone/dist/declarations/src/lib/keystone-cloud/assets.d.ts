import { Readable } from 'stream';
import { ImageMetadata } from '../../types';
export declare const buildKeystoneCloudImageSrc: ({ apiKey, filename, imagesDomain, }: {
    apiKey: string;
    filename: string;
    imagesDomain: string;
}) => Promise<string>;
export declare const buildKeystoneCloudFileSrc: ({ apiKey, graphqlApiEndpoint, filename, }: {
    apiKey: string;
    graphqlApiEndpoint: string;
    filename: string;
}) => Promise<string>;
export declare const getImageMetadataFromKeystoneCloud: ({ apiKey, filename, restApiEndpoint, }: {
    apiKey: string;
    filename: string;
    restApiEndpoint: string;
}) => Promise<ImageMetadata>;
export declare const getFileFromKeystoneCloud: ({ apiKey, filename, restApiEndpoint, }: {
    apiKey: string;
    filename: string;
    restApiEndpoint: string;
}) => Promise<{
    filesize: number;
}>;
export declare const uploadImageToKeystoneCloud: ({ apiKey, stream, filename, restApiEndpoint, }: {
    apiKey: string;
    stream: Readable;
    filename: string;
    restApiEndpoint: string;
}) => Promise<ImageMetadata>;
export declare const uploadFileToKeystoneCloud: ({ apiKey, stream, filename, restApiEndpoint, }: {
    apiKey: string;
    stream: Readable;
    filename: string;
    restApiEndpoint: string;
}) => Promise<any>;
