/** @jsxRuntime classic */
/** @jsx jsx */
import { jsx } from '@keystone-ui/core';
import { FieldProps, ListMeta } from '../../../../../types';
import { controller } from '../index';
export declare function Cards({ localList, field, foreignList, id, value, onChange, forceValidation, }: {
    foreignList: ListMeta;
    localList: ListMeta;
    field: ReturnType<typeof controller> & {
        display: {
            mode: 'cards';
        };
    };
    id: string;
    forceValidation: boolean | undefined;
    value: {
        kind: 'cards-view';
    };
} & Pick<FieldProps<typeof controller>, 'value' | 'onChange'>): jsx.JSX.Element;
