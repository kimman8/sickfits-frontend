import { DateType } from '@keystone-ui/fields';
export declare function isValidDate(date: DateType): boolean;
export declare function isValidTime(time: string): boolean;
export declare function isValidISO(value: {
    dateValue: DateType;
    timeValue: string;
}): boolean;
export declare function constructTimestamp({ dateValue, timeValue, }: {
    dateValue: string;
    timeValue: string;
}): string;
export declare function deconstructTimestamp(value: string): {
    dateValue: string;
    timeValue: string;
};
export declare function formatOutput(value: string): string;
export declare function resolveInitialTimeValue(value?: string, defaultValue?: string): string;
