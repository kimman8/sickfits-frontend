export declare type ToneTypes = 'positive' | 'negative' | 'warning' | 'help';
declare type Common = {
    title: string;
    message?: string;
    tone: ToneTypes;
};
declare type NeedResolution = {
    preserve: boolean;
    id: string;
};
export declare type ToastProps = Common & Partial<NeedResolution>;
export declare type ToastPropsExact = Common & NeedResolution;
export {};
