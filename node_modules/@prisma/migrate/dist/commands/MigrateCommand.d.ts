import { Command, Commands, HelpError } from '@prisma/sdk';
export declare class MigrateCommand implements Command {
    private readonly cmds;
    static new(cmds: Commands): MigrateCommand;
    private static help;
    private constructor();
    parse(argv: string[]): Promise<string | Error>;
    help(error?: string): string | HelpError;
}
