declare type Action = {
    action: () => void;
    label: string;
};
export declare type ActionsType = {
    cancel: Action;
    confirm: Action & {
        loading?: boolean;
    };
};
export declare type TransitionState = 'entering' | 'entered' | 'exiting' | 'exited';
export {};
