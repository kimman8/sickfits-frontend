import { FileMap } from '../types';
export declare function printFiles(printPath: string, files: FileMap): string;
export declare function printFilesFromMigrationIds(directory: string, migrationIds: string[], files: FileMap): string;
