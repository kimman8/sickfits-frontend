import { Command, HelpError } from '@prisma/sdk';
export declare class DbPull implements Command {
    static new(): DbPull;
    private static help;
    private printUrlAsDatasource;
    parse(argv: string[]): Promise<string | Error>;
    help(error?: string): string | HelpError;
}
