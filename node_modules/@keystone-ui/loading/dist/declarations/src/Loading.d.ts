/** @jsxRuntime classic */
/** @jsx jsx */
import { jsx } from '@keystone-ui/core';
export declare const loadingSizeValues: readonly ["large", "medium", "small"];
export declare const loadingToneValues: readonly ["active", "passive", "positive", "warning", "negative", "help"];
export declare type SizeKey = typeof loadingSizeValues[number];
export declare type ToneKey = typeof loadingToneValues[number];
declare type Props = {
    /** The aria-label for screen readers. */
    label: string;
    /** The color of the loading indicator. */
    tone?: ToneKey;
    /** The size of the loading indicator. */
    size?: SizeKey;
};
export declare const LoadingDots: ({ label, tone: toneKey, size: sizeKey, ...props }: Props) => jsx.JSX.Element;
export {};
