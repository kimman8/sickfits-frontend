import { AssetMode, ImageExtension } from '../../../types';
export declare const getImageRef: (mode: AssetMode, id: string, extension: ImageExtension) => string;
export declare const SUPPORTED_IMAGE_EXTENSIONS: string[];
export declare const parseImageRef: (ref: string) => {
    mode: AssetMode;
    id: string;
    extension: ImageExtension;
} | undefined;
