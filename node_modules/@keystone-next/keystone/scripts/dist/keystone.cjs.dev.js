'use strict';

var meow = require('meow');
var Path = require('path');
var url = require('url');
var express = require('express');
var fs = require('fs-extra');
var fastGlob = require('fast-glob');
var prettier = require('prettier');
var resolve = require('resolve');
var createSystem = require('../../dist/createSystem-558f626f.cjs.dev.js');
var migrations = require('../../dist/migrations-e3f537e1.cjs.dev.js');
var initConfig = require('../../dist/initConfig-d774ef5b.cjs.dev.js');
var _objectSpread = require('@babel/runtime/helpers/objectSpread2');
var pirates = require('pirates');
var babel = require('@babel/core');
var sourceMapSupport = require('source-map-support');
var createAdminUIMiddleware = require('../../dist/createAdminUIMiddleware-7ca43618.cjs.dev.js');
var artifacts_dist_keystone = require('../../artifacts/dist/keystone.cjs.dev.js');
var utils = require('../../dist/utils-de2e2c01.cjs.dev.js');
var execa = require('execa');
require('../../dist/core-c8ecce23.cjs.dev.js');
require('../../dist/sqlite-b63545bc.cjs.dev.js');
require('decimal.js');
require('@graphql-ts/schema/api-without-context');
require('@graphql-ts/schema');
require('graphql-type-json');
require('graphql-upload/public/GraphQLUpload.js');
require('@graphql-ts/schema/api-with-context');
require('@babel/runtime/helpers/objectWithoutProperties');
require('graphql');
require('@emotion/hash');
require('../../dist/admin-meta-graphql-1e30b380.cjs.dev.js');
require('@apollo/client');
require('../../session/dist/keystone.cjs.dev.js');
require('cookie');
require('@hapi/iron');
require('uid-safe');
require('@babel/runtime/helpers/classPrivateFieldInitSpec');
require('@babel/runtime/helpers/classPrivateFieldGet');
require('@babel/runtime/helpers/classPrivateFieldSet');
require('p-limit');
require('pluralize');
require('apollo-server-errors');
require('uuid');
require('image-type');
require('image-size');
require('../../fields/types/image/utils/dist/keystone.cjs.dev.js');
require('node-fetch');
require('form-data');
require('crypto');
require('stream');
require('filenamify');
require('@sindresorhus/slugify');
require('../../fields/types/file/utils/dist/keystone.cjs.dev.js');
require('@prisma/sdk');
require('@prisma/migrate');
require('chalk');
require('cuid');
require('../../dist/package-path-d511a573.cjs.dev.js');
require('cors');
require('graphql-upload');
require('../../dist/createApolloServer-8725cc81.cjs.dev.js');
require('apollo-server-micro');
require('apollo-server-express');
require('prompts');
require('@babel/runtime/helpers/defineProperty');

function _interopDefault (e) { return e && e.__esModule ? e : { 'default': e }; }

function _interopNamespace(e) {
  if (e && e.__esModule) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () {
            return e[k];
          }
        });
      }
    });
  }
  n['default'] = e;
  return Object.freeze(n);
}

var meow__default = /*#__PURE__*/_interopDefault(meow);
var Path__default = /*#__PURE__*/_interopDefault(Path);
var url__default = /*#__PURE__*/_interopDefault(url);
var express__default = /*#__PURE__*/_interopDefault(express);
var fs__namespace = /*#__PURE__*/_interopNamespace(fs);
var fastGlob__default = /*#__PURE__*/_interopDefault(fastGlob);
var prettier__default = /*#__PURE__*/_interopDefault(prettier);
var resolve__default = /*#__PURE__*/_interopDefault(resolve);
var babel__namespace = /*#__PURE__*/_interopNamespace(babel);
var sourceMapSupport__default = /*#__PURE__*/_interopDefault(sourceMapSupport);
var execa__default = /*#__PURE__*/_interopDefault(execa);

const formatSource$1 = (src, parser = 'babel') => prettier__default['default'].format(src, {
  parser,
  trailingComma: 'es5',
  singleQuote: true
});

function getDoesAdminConfigExist() {
  try {
    const configPath = Path__default['default'].join(process.cwd(), 'admin', 'config');
    resolve__default['default'].sync(configPath, {
      extensions: ['.ts', '.tsx', '.js'],
      preserveSymlinks: false
    });
    return true;
  } catch (err) {
    if (err.code === 'MODULE_NOT_FOUND') {
      return false;
    }

    throw err;
  }
}

async function writeAdminFile$1(file, projectAdminPath) {
  const outputFilename = Path__default['default'].join(projectAdminPath, file.outputPath);

  if (file.mode === 'copy') {
    if (!Path__default['default'].isAbsolute(file.inputPath)) {
      throw new Error(`An inputPath of "${file.inputPath}" was provided to copy but inputPaths must be absolute`);
    }

    await fs__namespace['default'].ensureDir(Path__default['default'].dirname(outputFilename)); // TODO: should we use copyFile or copy?

    await fs__namespace['default'].copyFile(file.inputPath, outputFilename);
  }

  if (file.mode === 'write') {
    await fs__namespace['default'].outputFile(outputFilename, formatSource$1(file.src));
  }

  return Path__default['default'].normalize(outputFilename);
}

const generateAdminUI = async (config, graphQLSchema, adminMeta, projectAdminPath) => {
  var _config$ui$getAdditio, _config$ui, _config$ui$getAdditio2;

  // Nuke any existing files in our target directory
  await fs__namespace['default'].remove(projectAdminPath);
  const publicDirectory = Path__default['default'].join(projectAdminPath, 'public');

  if (config.images || config.files) {
    await fs__namespace['default'].mkdir(publicDirectory, {
      recursive: true
    });
  }

  if (config.images) {
    var _config$images$local$, _config$images$local;

    const storagePath = Path__default['default'].resolve((_config$images$local$ = (_config$images$local = config.images.local) === null || _config$images$local === void 0 ? void 0 : _config$images$local.storagePath) !== null && _config$images$local$ !== void 0 ? _config$images$local$ : './public/images');
    await fs__namespace['default'].mkdir(storagePath, {
      recursive: true
    });
    await fs__namespace['default'].symlink(Path__default['default'].relative(publicDirectory, storagePath), Path__default['default'].join(publicDirectory, 'images'), 'junction');
  }

  if (config.files) {
    var _config$files$local$s, _config$files$local;

    const storagePath = Path__default['default'].resolve((_config$files$local$s = (_config$files$local = config.files.local) === null || _config$files$local === void 0 ? void 0 : _config$files$local.storagePath) !== null && _config$files$local$s !== void 0 ? _config$files$local$s : './public/files');
    await fs__namespace['default'].mkdir(storagePath, {
      recursive: true
    });
    await fs__namespace['default'].symlink(Path__default['default'].relative(publicDirectory, storagePath), Path__default['default'].join(publicDirectory, 'files'), 'junction');
  } // Write out the files configured by the user


  const userPages = (_config$ui$getAdditio = (_config$ui = config.ui) === null || _config$ui === void 0 ? void 0 : (_config$ui$getAdditio2 = _config$ui.getAdditionalFiles) === null || _config$ui$getAdditio2 === void 0 ? void 0 : _config$ui$getAdditio2.map(x => x(config))) !== null && _config$ui$getAdditio !== void 0 ? _config$ui$getAdditio : [];
  const userFilesToWrite = (await Promise.all(userPages)).flat();
  const savedFiles = await Promise.all(userFilesToWrite.map(file => writeAdminFile$1(file, projectAdminPath)));
  const uniqueFiles = new Set(savedFiles); // Write out the built-in admin UI files. Don't overwrite any user-defined pages.

  const configFileExists = getDoesAdminConfigExist();
  const adminFiles = createSystem.writeAdminFiles(config, graphQLSchema, adminMeta, configFileExists, projectAdminPath);
  await Promise.all(adminFiles.filter(x => !uniqueFiles.has(Path__default['default'].normalize(Path__default['default'].join(projectAdminPath, x.outputPath)))).map(file => writeAdminFile$1(file, projectAdminPath))); // Add files to pages/ which point to any files which exist in admin/pages

  const userPagesDir = Path__default['default'].join(process.cwd(), 'admin', 'pages');
  const files = await fastGlob__default['default']('**/*.{js,jsx,ts,tsx}', {
    cwd: userPagesDir
  });
  await Promise.all(files.map(async filename => {
    const outputFilename = Path__default['default'].join(projectAdminPath, 'pages', filename);
    const path = Path__default['default'].relative(Path__default['default'].dirname(outputFilename), Path__default['default'].join(userPagesDir, filename));
    const importPath = createSystem.serializePathForImport(path);
    await fs__namespace['default'].outputFile(outputFilename, `export { default } from ${importPath}`);
  }));
};

async function buildAdminUI(projectAdminPath) {
  // importing next/dist/build is quite expensive so we're requiring it lazily

  /** We do this to stop webpack from bundling next inside of next */
  const next = 'next/dist/build';

  const build = require(next).default;

  await build(projectAdminPath);
}

const EXTENSIONS = ['.js', '.jsx', '.ts', '.tsx'];
const babelPlugins = [require.resolve('@babel/plugin-transform-modules-commonjs')];

const hook = () => {
  let compiling = false;
  let sourceMaps = {};
  let needsToInstallSourceMapSupport = true;

  function compileHook(code, filename) {
    if (compiling) return code; // we do this lazily because jest has its own require implementation
    // which means preconstruct's require hook won't be run
    // so we don't want to install source map support because that will mess up
    // jest's source map support

    if (needsToInstallSourceMapSupport) {
      sourceMapSupport__default['default'].install({
        environment: 'node',

        retrieveSourceMap(source) {
          let map = sourceMaps[source];

          if (map !== undefined) {
            return {
              url: source,
              map
            };
          } else {
            return null;
          }
        }

      });
      needsToInstallSourceMapSupport = false;
    }

    try {
      compiling = true;
      const partialConfig = babel__namespace.loadPartialConfig({
        plugins: babelPlugins,
        filename,
        sourceMaps: 'both',
        rootMode: 'upward-optional'
      });
      let options = partialConfig.options;

      if (!partialConfig.hasFilesystemConfig()) {
        options = _objectSpread(_objectSpread({}, options), {}, {
          // note that we're explicitly removing the plugin(@babel/plugin-transform-modules-commonjs)
          // we added above because for some reason, it interacts poorly with next/babel
          // and results in stray ESM imports of React when they should be CJS
          // note that we're never going to be removing a consumer's Babel config since
          // that would make hasFilesystemConfig() return true
          plugins: [],
          presets: [require.resolve('next/babel')]
        });
      }

      const output = babel__namespace.transformSync(code, options);
      sourceMaps[filename] = output.map;
      return output.code;
    } finally {
      compiling = false;
    }
  }

  return pirates.addHook(compileHook, {
    exts: EXTENSIONS
  });
};

const requireSource = filePath => {
  const unregister = hook();

  const result = require(filePath);

  unregister();
  return result;
};

const devLoadingHTMLFilepath = Path__default['default'].join(Path__default['default'].dirname(require.resolve('@keystone-next/keystone/package.json')), 'static', 'dev-loading.html');
const dev = async (cwd, shouldDropDatabase) => {
  var _config$server, _config$server2;

  console.log('✨ Starting Keystone');
  const app = express__default['default']();
  let expressServer = null;
  let adminUIMiddleware = null;

  const ready = () => !!(expressServer && adminUIMiddleware);

  let disconnect = null;
  const config = initConfig.initConfig(requireSource(utils.getConfigPath(cwd)).default);

  const initKeystone = async () => {
    var _config$ui;

    const {
      graphQLSchema,
      adminMeta,
      getKeystone
    } = createSystem.createSystem(config); // Generate the Artifacts

    console.log('✨ Generating GraphQL and Prisma schemas');
    const prismaSchema = (await artifacts_dist_keystone.generateCommittedArtifacts(graphQLSchema, config, cwd)).prisma;
    await artifacts_dist_keystone.generateNodeModulesArtifacts(graphQLSchema, config, cwd); // Set up the Database

    if (config.db.useMigrations) {
      await migrations.devMigrations(config.db.url, prismaSchema, artifacts_dist_keystone.getSchemaPaths(cwd).prisma, shouldDropDatabase);
    } else {
      await migrations.pushPrismaSchemaToDatabase(config.db.url, prismaSchema, artifacts_dist_keystone.getSchemaPaths(cwd).prisma, shouldDropDatabase);
    }

    const prismaClient = artifacts_dist_keystone.requirePrismaClient(cwd);
    const keystone = getKeystone(prismaClient);
    const {
      createContext
    } = keystone; // Connect to the Database

    console.log('✨ Connecting to the database');
    await keystone.connect();

    disconnect = () => keystone.disconnect(); // Set up the Express Server


    console.log('✨ Creating server');
    expressServer = await createAdminUIMiddleware.createExpressServer(config, graphQLSchema, createContext);
    console.log(`✅ GraphQL API ready`); // Initialise the Admin UI

    if (!((_config$ui = config.ui) !== null && _config$ui !== void 0 && _config$ui.isDisabled)) {
      console.log('✨ Generating Admin UI code');
      await generateAdminUI(config, graphQLSchema, adminMeta, utils.getAdminPath(cwd));
      console.log('✨ Preparing Admin UI app');
      adminUIMiddleware = await createAdminUIMiddleware.createAdminUIMiddleware(config, createContext, true, utils.getAdminPath(cwd));
      expressServer.use(adminUIMiddleware);
      console.log(`✅ Admin UI ready`);
    }
  }; // You shouldn't really be doing a healthcheck on the dev server, but we
  // respond on the endpoint with the correct error code just in case. This
  // doesn't send the configured data shape, because config doesn't allow
  // for the "not ready" case but that's probably OK.


  if ((_config$server = config.server) !== null && _config$server !== void 0 && _config$server.healthCheck) {
    const healthCheckPath = config.server.healthCheck === true ? createAdminUIMiddleware.defaults.healthCheckPath : config.server.healthCheck.path || createAdminUIMiddleware.defaults.healthCheckPath;
    app.use(healthCheckPath, (req, res, next) => {
      if (expressServer) return next();
      res.status(503).json({
        status: 'fail',
        timestamp: Date.now()
      });
    });
  } // Serve the dev status page for the Admin UI


  app.use('/__keystone_dev_status', (req, res) => {
    res.json({
      ready: ready() ? true : false
    });
  }); // Pass the request the express server, or serve the loading page

  app.use((req, res, next) => {
    var _config$graphql;

    // If both the express server and Admin UI Middleware are ready, we're go!
    if (expressServer && adminUIMiddleware) return expressServer(req, res, next); // Otherwise, we may be able to serve the GraphQL API

    const {
      pathname
    } = url__default['default'].parse(req.url);

    if (expressServer && pathname === (((_config$graphql = config.graphql) === null || _config$graphql === void 0 ? void 0 : _config$graphql.path) || '/api/graphql')) {
      return expressServer(req, res, next);
    } // Serve the loading page


    res.sendFile(devLoadingHTMLFilepath);
  });
  const port = ((_config$server2 = config.server) === null || _config$server2 === void 0 ? void 0 : _config$server2.port) || process.env.PORT || 3000;
  let initKeystonePromiseResolve;
  let initKeystonePromiseReject;
  let initKeystonePromise = new Promise((resolve, reject) => {
    initKeystonePromiseResolve = resolve;
    initKeystonePromiseReject = reject;
  });
  const server = app.listen(port, err => {
    if (err) throw err;
    console.log(`⭐️ Dev Server Ready on http://localhost:${port}`); // We start initialising Keystone after the dev server is ready,
    // otherwise it slows down the first response significantly

    initKeystone().then(() => {
      initKeystonePromiseResolve();
    }).catch(err => {
      server.close(async closeErr => {
        if (closeErr) {
          console.log('There was an error while closing the server');
          console.log(closeErr);
        }

        try {
          var _disconnect;

          await ((_disconnect = disconnect) === null || _disconnect === void 0 ? void 0 : _disconnect());
        } catch (err) {
          console.log('There was an error while disconnecting from the database');
          console.log(err);
        }

        initKeystonePromiseReject(err);
      });
    });
  });
  await initKeystonePromise;
  return () => new Promise((resolve, reject) => {
    server.close(async err => {
      try {
        var _disconnect2;

        await ((_disconnect2 = disconnect) === null || _disconnect2 === void 0 ? void 0 : _disconnect2());
      } catch (disconnectionError) {
        if (!err) {
          err = disconnectionError;
        } else {
          console.log('There was an error while disconnecting from the database');
          console.log(disconnectionError);
        }
      }

      if (err) {
        reject(err);
      } else {
        resolve();
      }
    });
  });
};

const start = async cwd => {
  var _config$ui, _config$server;

  console.log('✨ Starting Keystone'); // This is the compiled version of the configuration which was generated during the build step.
  // See reexportKeystoneConfig().

  const apiFile = Path__default['default'].join(utils.getAdminPath(cwd), '.next/server/pages/api/__keystone_api_build.js');

  if (!fs__namespace.existsSync(apiFile)) {
    console.log('🚨 keystone-next build must be run before running keystone-next start');
    throw new utils.ExitError(1);
  }

  const config = initConfig.initConfig(require(apiFile).config);
  const {
    getKeystone,
    graphQLSchema
  } = createSystem.createSystem(config);
  const prismaClient = artifacts_dist_keystone.requirePrismaClient(cwd);
  const keystone = getKeystone(prismaClient);
  console.log('✨ Connecting to the database');
  await keystone.connect();
  console.log('✨ Creating server');
  const server = await createAdminUIMiddleware.createExpressServer(config, graphQLSchema, keystone.createContext);
  console.log(`✅ GraphQL API ready`);

  if (!((_config$ui = config.ui) !== null && _config$ui !== void 0 && _config$ui.isDisabled)) {
    console.log('✨ Preparing Admin UI Next.js app');
    server.use(await createAdminUIMiddleware.createAdminUIMiddleware(config, keystone.createContext, false, utils.getAdminPath(cwd)));
    console.log(`✅ Admin UI ready`);
  }

  const port = ((_config$server = config.server) === null || _config$server === void 0 ? void 0 : _config$server.port) || process.env.PORT || 3000;
  server.listen(port, err => {
    if (err) throw err;
    console.log(`⭐️ Server Ready on http://localhost:${port}`);
  });
};

async function writeAdminFile(file, projectAdminPath) {
  const outputFilename = Path__default['default'].join(projectAdminPath, file.outputPath);

  if (file.mode === 'copy') {
    if (!Path__default['default'].isAbsolute(file.inputPath)) {
      throw new Error(`An inputPath of "${file.inputPath}" was provided to copy but inputPaths must be absolute`);
    }

    await fs__namespace['default'].ensureDir(Path__default['default'].dirname(outputFilename)); // TODO: should we use copyFile or copy?

    await fs__namespace['default'].copyFile(file.inputPath, outputFilename);
  }

  if (file.mode === 'write') {
    await fs__namespace['default'].outputFile(outputFilename, formatSource(file.src));
  }
} // FIXME: Duplicated from admin-ui package. Need to decide on a common home.


function serializePathForImport(path) {
  // JSON.stringify is important here because it will escape windows style paths(and any thing else that might potentionally be in there)
  return JSON.stringify(path // Next is unhappy about imports that include .ts/tsx in them because TypeScript is unhappy with them becasue when doing a TypeScript compilation with tsc, the imports won't be written so they would be wrong there
  .replace(/\.tsx?$/, ''));
} // FIXME: Duplicated from admin-ui package. Need to decide on a common home.

const formatSource = (src, parser = 'babel') => prettier__default['default'].format(src, {
  parser,
  trailingComma: 'es5',
  singleQuote: true
});

const reexportKeystoneConfig = async (cwd, isDisabled) => {
  const projectAdminPath = utils.getAdminPath(cwd);
  const configPath = utils.getConfigPath(cwd);

  if (isDisabled) {
    // Nuke any existing files in our target directory
    await fs__namespace['default'].remove(projectAdminPath);
  } // We re-export the Keystone config file into the Admin UI project folder
  // so that when we run the build step, we will end up with a compiled version
  // of the configuration file in the .next/ directory. Even if we're not building
  // an Admin UI, we still need to run the `build()` function so that this config
  // file is correctly compiled.


  const pkgDir = Path__default['default'].dirname(require.resolve('@keystone-next/keystone/package.json'));
  const p = serializePathForImport(Path__default['default'].relative(Path__default['default'].join(projectAdminPath, 'pages', 'api'), configPath));
  const files = [{
    mode: 'write',
    src: `export { default as config } from ${p};
            export default function (req, res) { return res.status(500) }`,
    outputPath: Path__default['default'].join('pages', 'api', '__keystone_api_build.js')
  }];

  if (isDisabled) {
    // These are the basic files required to have a valid Next.js project. If the
    // Admin UI is disabled then we need to do this ourselves here.
    files.push({
      mode: 'copy',
      inputPath: Path__default['default'].join(pkgDir, 'static', 'next.config.js'),
      outputPath: 'next.config.js'
    }, {
      mode: 'copy',
      inputPath: Path__default['default'].join(pkgDir, 'static', 'tsconfig.json'),
      outputPath: 'tsconfig.json'
    });
  }

  await Promise.all(files.map(file => writeAdminFile(file, projectAdminPath)));
};

async function build(cwd) {
  var _config$ui, _config$ui2;

  const config = initConfig.initConfig(requireSource(utils.getConfigPath(cwd)).default);
  const {
    graphQLSchema,
    adminMeta
  } = createSystem.createSystem(config);
  await artifacts_dist_keystone.validateCommittedArtifacts(graphQLSchema, config, cwd);
  console.log('✨ Building Keystone'); // FIXME: This needs to generate clients for the correct build target using binaryTarget
  // https://www.prisma.io/docs/reference/api-reference/prisma-schema-reference#binarytargets-options

  await artifacts_dist_keystone.generateNodeModulesArtifacts(graphQLSchema, config, cwd);

  if ((_config$ui = config.ui) !== null && _config$ui !== void 0 && _config$ui.isDisabled) {
    console.log('✨ Skipping Admin UI code generation');
  } else {
    console.log('✨ Generating Admin UI code');
    await generateAdminUI(config, graphQLSchema, adminMeta, utils.getAdminPath(cwd));
  }

  console.log('✨ Generating Keystone config code');
  await reexportKeystoneConfig(cwd, (_config$ui2 = config.ui) === null || _config$ui2 === void 0 ? void 0 : _config$ui2.isDisabled);
  console.log('✨ Building Admin UI');
  await buildAdminUI(utils.getAdminPath(cwd));
}

async function prisma(cwd, args) {
  const config = initConfig.initConfig(requireSource(utils.getConfigPath(cwd)).default);
  const {
    graphQLSchema
  } = createSystem.createSystem(config);
  await artifacts_dist_keystone.validateCommittedArtifacts(graphQLSchema, config, cwd);
  await artifacts_dist_keystone.generateNodeModulesArtifacts(graphQLSchema, config, cwd);
  const result = await execa__default['default']('node', [require.resolve('prisma'), ...args], {
    cwd,
    stdio: 'inherit',
    reject: false,
    env: _objectSpread(_objectSpread({}, process.env), {}, {
      DATABASE_URL: config.db.url,
      PRISMA_HIDE_UPDATE_MESSAGE: '1'
    })
  });

  if (result.exitCode !== 0) {
    throw new utils.ExitError(result.exitCode);
  }
}

// There’s some files that we need to generate into node_modules and it’s
// important to have them available immediately so things like TypeScript
// won’t fail.
// We want to validate that your Prisma and GraphQL schemas are up to date
// to prevent the awkward “this is changing because of a previous PR that
// didn’t update these things”.
// Why do validation in the postinstall rather than a separate validate command?
//
// It means that it’s hard to get it wrong. You have to run the postinstall
// script anyway so it prevents “oh no, you forgot add this to your CI”
// node_modules
//   .prisma/client (this is where Prisma generates the client to by default,
//      this means we can have a conversation about whether we should tell
//      people to use @prisma/client directly for certain things though we
//      are not necessarily saying that’s what we should do)
//   .keystone
//     - All .js files will have a corresponding .d.ts file for TypeScript.
//       We are generating vanilla JavaScript because:
//         * the user may not be using TypeScript
//         * we can’t/shouldn’t rely on files in node_modules being transpiled even if they are
//     - types.{js,.ts}: .d.ts will be the same as current .keystone/schema-types.ts, the .js will be empty
//     - next/graphql-api.js: to be imported into a next app as an api route
//         * only generated with generateNextGraphqlAPI option
//     - api.js:  includes the lists API (full exports to be decided elsewhere
//         * only generated with generateNodeAPI option

async function postinstall(cwd, shouldFix) {
  const config = initConfig.initConfig(requireSource(utils.getConfigPath(cwd)).default);
  const {
    graphQLSchema
  } = createSystem.createSystem(config);

  if (shouldFix) {
    await artifacts_dist_keystone.generateCommittedArtifacts(graphQLSchema, config, cwd);
    console.log('✨ Generated GraphQL and Prisma schemas');
  } else {
    await artifacts_dist_keystone.validateCommittedArtifacts(graphQLSchema, config, cwd);
    console.log('✨ GraphQL and Prisma schemas are up to date');
  }

  await artifacts_dist_keystone.generateNodeModulesArtifacts(graphQLSchema, config, cwd);
}

const commands = {
  dev,
  start,
  build,
  prisma,
  postinstall
};
async function cli(cwd, argv) {
  const {
    input,
    help,
    flags
  } = meow__default['default'](`
    Usage
      $ keystone-next [command]
    Commands
        dev           start the project in development mode (default)
        postinstall   generate client APIs and types (optional)
        build         build the project (must be done before using start)
        start         start the project in production mode
        prisma        run Prisma CLI commands safely
    `, {
    flags: {
      fix: {
        default: false,
        type: 'boolean'
      },
      resetDb: {
        default: false,
        type: 'boolean'
      }
    },
    argv
  });
  const command = input[0] || 'dev';

  if (!isCommand(command)) {
    console.log(`${command} is not a command that keystone-next accepts`);
    console.log(help);
    throw new utils.ExitError(1);
  }

  if (command === 'prisma') {
    return prisma(cwd, argv.slice(1));
  } else if (command === 'postinstall') {
    return postinstall(cwd, flags.fix);
  } else if (command === 'dev') {
    return dev(cwd, flags.resetDb);
  } else {
    return commands[command](cwd);
  }
}

function isCommand(command) {
  return command in commands;
}

cli(process.cwd(), process.argv.slice(2)).catch(err => {
  if (err instanceof utils.ExitError) {
    process.exit(err.code);
  }

  console.log(err);
  process.exit(1);
});
