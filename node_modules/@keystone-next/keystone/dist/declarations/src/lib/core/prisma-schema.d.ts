import { DatabaseProvider } from '../../types';
import { ListsWithResolvedRelations } from './resolve-relationships';
export declare function printPrismaSchema(lists: ListsWithResolvedRelations, provider: DatabaseProvider, clientDir: string): string;
