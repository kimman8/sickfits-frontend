/// <reference types="node" />
import type { IncomingMessage } from 'http';
import { GraphQLSchema } from 'graphql';
import { SessionContext, KeystoneContext, GqlNames, KeystoneConfig } from '../../types';
import { PrismaClient } from '../core/utils';
import { InitialisedList } from '../core/types-for-lists';
export declare function makeCreateContext({ graphQLSchema, internalSchema, prismaClient, gqlNamesByList, config, lists, }: {
    graphQLSchema: GraphQLSchema;
    internalSchema: GraphQLSchema;
    config: KeystoneConfig;
    prismaClient: PrismaClient;
    gqlNamesByList: Record<string, GqlNames>;
    lists: Record<string, InitialisedList>;
}): ({ sessionContext, skipAccessControl, req, schemaName, }?: {
    sessionContext?: SessionContext<any> | undefined;
    skipAccessControl?: boolean | undefined;
    req?: IncomingMessage | undefined;
    schemaName?: "public" | "internal" | undefined;
}) => KeystoneContext;
