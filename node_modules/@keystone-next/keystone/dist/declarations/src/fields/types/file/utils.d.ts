import { AssetMode } from '../../../types';
export declare const getFileRef: (mode: AssetMode, name: string) => string;
export declare const parseFileRef: (ref: string) => {
    mode: AssetMode;
    filename: string;
} | undefined;
