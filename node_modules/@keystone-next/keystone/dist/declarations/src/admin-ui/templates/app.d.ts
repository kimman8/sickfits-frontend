import { GraphQLSchema } from 'graphql';
import { AdminMetaRootVal } from '../../types';
declare type AppTemplateOptions = {
    configFileExists: boolean;
    projectAdminPath: string;
};
export declare const appTemplate: (adminMetaRootVal: AdminMetaRootVal, graphQLSchema: GraphQLSchema, { configFileExists, projectAdminPath }: AppTemplateOptions, apiPath: string) => string;
export {};
